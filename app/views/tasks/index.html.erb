<h1>My Tasks</h1>
<form>
  <% @tasks.each do |task|%>
    <input type="checkbox" name="<%=task.id%>" <%= "checked" if task.completed == true%>>
    <label for="<%=task.id%>"><%= link_to task.title, task_path(task.id) %></label><br>
  <%end%>
</form>

<%=link_to "Create new task", new_path()%>

